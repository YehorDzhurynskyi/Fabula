#pragma once

#include "Fabula/types.h"

using fblProgramID = fblU32;
using fblShaderID = fblU32;
using fblTextureID = fblU32;
using fblShaderLocationID = fblU32;

using fblVertexArrayID = fblU32;

using fblBufferID = fblU32;
using fblVertexBufferID = fblBufferID;
using fblIndexBufferID = fblBufferID;
using fblFrameBufferID = fblBufferID;
